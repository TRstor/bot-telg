# البدء مع معرض الشعبيات

✅ تم إنشاء المشروع بنجاح!

## 📦 ما تم إنشاؤه

### الملفات الأساسية:
- ✅ `app/gallery/page.jsx` - صفحة المعرض
- ✅ `components/GalleryScript.js` - سكريبت المعرض الرئيسي
- ✅ `utils/galleryCommands.js` - أوامر البوت
- ✅ `utils/botIntegration.js` - تكامل بوت تليجرام

### ملفات الإعداد:
- ✅ `package.json` - إدارة المكتبات
- ✅ `next.config.js` - إعدادات Next.js
- ✅ `app/layout.js` - التخطيط الرئيسي
- ✅ `.gitignore` - استثناءات Git

### ملفات التوثيق:
- ✅ `README.md` - دليل المشروع
- ✅ `USAGE.md` - دليل الاستخدام
- ✅ `EXAMPLES.md` - أمثلة الاستخدام
- ✅ `SETUP.md` - هذا الملف

---

## 🚀 الخطوات التالية

### 1️⃣ تثبيت المكتبات
```bash
cd /workspaces/bot-telg
npm install
```

### 2️⃣ تشغيل المشروع
```bash
npm run dev
```

سيظهر:
```
> ready - started server on 0.0.0.0:3000
```

افتح: http://localhost:3000

### 3️⃣ اختبار المعرض
- اضغط على زر "فتح معرض الشعبيات"
- جرّب البحث والتصفية
- أضف صور للمفضلة

---

## 🔗 ربط البوت

### إذا كنت تستخدم Node.js:

```bash
npm install node-telegram-bot-api
```

ثم استخدم `utils/botIntegration.js`

### إذا كنت تستخدم Python:

```bash
pip install pyTelegramBotAPI
```

راجع `EXAMPLES.md` للأمثلة

---

## 📝 إضافة الصور

عدّل `components/GalleryScript.js`:

```javascript
var topImages = [
  "https://i.ibb.co/...",  // أضف الروابط هنا
  "https://i.ibb.co/..."
];

var IMAGE_META = {
  "https://i.ibb.co/...": { name: "اسم الصورة", keywords: [] }
};
```

---

## 🛠️ الأوامر المتاحة

```bash
npm run dev      # تطوير
npm run build    # بناء
npm start        # إنتاج
npm run lint     # فحص
```

---

## 📂 هيكل المشروع

```
bot-telg/
├── app/
│   ├── gallery/
│   │   └── page.jsx
│   ├── layout.js
│   └── page.js
├── components/
│   ├── GalleryPopup.jsx
│   └── GalleryScript.js
├── utils/
│   ├── galleryCommands.js
│   └── botIntegration.js
├── .gitignore
├── package.json
├── next.config.js
├── README.md
├── USAGE.md
├── EXAMPLES.md
└── SETUP.md
```

---

## 🎯 الروابط الرئيسية

| الصفحة | الرابط |
|--------|---------|
| الرئيسية | http://localhost:3000 |
| المعرض | http://localhost:3000/gallery |

---

## ❓ أسئلة شائعة

**س: كيف أضيف صور جديدة؟**
ج: عدّل `components/GalleryScript.js` وأضف روابط الصور والبيانات

**س: كيف أربط مع البوت؟**
ج: استخدم `utils/botIntegration.js` أو راجع `EXAMPLES.md`

**س: كيف أحفظ المفضلة؟**
ج: يُحفظ تلقائياً في localStorage

**س: هل أحتاج Firebase؟**
ج: الحالياً لا، لكن يمكن إضافتها لاحقاً

---

## 🎉 اكتمل الإعداد!

أنت جاهز الآن للبدء. جرّب المعرض وأضف الصور التي تريدها.

**نصيحة:** ابدأ بـ `npm run dev` وافتح http://localhost:3000/gallery

---

آخر تحديث: **December 24, 2025**
